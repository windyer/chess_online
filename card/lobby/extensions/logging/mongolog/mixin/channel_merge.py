import re
def channel_merge(channel):
    #if "zhiyifu" in channel:
    #    ind=channel.index("_",channel.index("_")+1)
    #    return  channel[0:ind+1]
    #    #ch = re.compile(r'[0-9]+_zhiyifu')
    #    #real_ch = ch.findall(channel)
    #    #if len(real_ch) > 0:
    #    #    return real_ch[0]
    #    #return channel
    #else:
    #    return channel
    return channel